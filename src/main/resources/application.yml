spring:
  application:
    name: service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ccut?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false
    username: root
    password: 123456
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
  servlet:
    multipart:
      max-file-size: 1024MB
      max-request-size: 1024MB
  ai:
    dashscope:
      api-key: sk-b79bfc6284eb467da714a7957eee7605
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1

server:
  port: 9999


mybatis:
  mapper-locations: classpath:mapper/*.xml
  # 开启驼峰命名
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    com.ccut.mapper: debug

