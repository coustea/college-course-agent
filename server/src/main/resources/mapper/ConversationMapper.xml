<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccut.mapper.ConversationMapper">

    <resultMap id="ConversationResultMap" type="com.ccut.entity.Conversation">
        <id property="id" column="id"/>
        <result property="conversationId" column="conversation_id"/>
        <result property="username" column="username"/>
        <result property="sequenceNum" column="sequence_num"/>
        <result property="title" column="title"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.ccut.entity.Conversation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO conversations (conversation_id, username, sequence_num, title)
        VALUES (#{conversationId}, #{username}, #{sequenceNum}, #{title})
    </insert>

    <select id="findByUsername" resultMap="ConversationResultMap">
        SELECT id, conversation_id, username, sequence_num, title, created_at, updated_at
        FROM conversations
        WHERE username = #{username}
        ORDER BY updated_at DESC
    </select>

    <select id="findByConversationId" resultMap="ConversationResultMap">
        SELECT id, conversation_id, username, sequence_num, title, created_at, updated_at
        FROM conversations
        WHERE conversation_id = #{conversationId}
    </select>

    <select id="getNextSequenceNum" resultType="int">
        SELECT COALESCE(MAX(sequence_num), 0) + 1
        FROM conversations
        WHERE username = #{username}
    </select>



    <update id="updateTitle">
        UPDATE conversations
        SET title = #{title}
        WHERE conversation_id = #{conversationId}
    </update>

    <delete id="deleteByConversationId">
        DELETE FROM conversations
        WHERE conversation_id = #{conversationId}
    </delete>

</mapper>
