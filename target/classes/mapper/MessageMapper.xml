<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccut.mapper.MessageMapper">

    <resultMap id="MessageResultMap" type="com.ccut.entity.Message">
        <id property="id" column="id"/>
        <result property="conversationId" column="conversation_id"/>
        <result property="role" column="role"/>
        <result property="content" column="content"/>
        <result property="sequenceNum" column="sequence_num"/>
        <result property="createdAt" column="created_at"/>
        <result property="tokensUsed" column="tokens_used"/>
    </resultMap>

    <insert id="insert" parameterType="com.ccut.entity.Message" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO messages (conversation_id, role, content, sequence_num, tokens_used)
        VALUES (#{conversationId}, #{role}, #{content}, #{sequenceNum}, #{tokensUsed})
    </insert>

    <select id="findByConversationId" resultMap="MessageResultMap">
        SELECT id, conversation_id, role, content, sequence_num, created_at, tokens_used
        FROM messages
        WHERE conversation_id = #{conversationId}
        ORDER BY sequence_num ASC
    </select>

    <select id="countByConversationId" resultType="int">
        SELECT COUNT(*)
        FROM messages
        WHERE conversation_id = #{conversationId}
    </select>

    <delete id="deleteByConversationId">
        DELETE FROM messages
        WHERE conversation_id = #{conversationId}
    </delete>

    <select id="getNextSequenceNum" resultType="int">
        SELECT COALESCE(MAX(sequence_num), 0) + 1
        FROM messages
        WHERE conversation_id = #{conversationId}
    </select>

</mapper>
